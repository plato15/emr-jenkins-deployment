pipeline {
    agent any 
    environment {
        GITHUB_TOKEN = credentials('GABE_GITHUB_TOKEN_TEST')
    }
    stages {
        stage('Build') {
            steps { 
                   echo "HI I am Build" 
            }
        }
        stage('Test') {
            steps {

            }
        }
        stage('Deploy') {
            steps {

            }
        }
    }
}
