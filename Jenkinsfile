pipeline {
    agent any 
    environment {
        GITHUB_TOKEN = "Nothing"
    }
    stages {
        stage('Build') {
            steps { 
                  sh 'echo HI I am Build'
            }
        }
        stage('Test') {
            steps {

            }
        }
        stage('Deploy') {
            steps {

            }
        }
    }
}
